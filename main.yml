- name: Deploy CloudFormation
  uses: aws-actions/aws-cloudformation-github-deploy@v1.2.0
  with:
    name: 'resumeprodstack'
    template: ./main.yml
    capabilities: CAPABILITY_NAMED_IAM
    parameter-overrides: |
      DummyParameter=$(date +%s)  # Always generate a unique timestamp


